language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-impact-node' ]; then cd .. && eval "mv $currentfolder generator-impact-node" && cd generator-impact-node; fi

